

- file paths in spec.json are relative to spec.json folder

- save step - src is optional, ==path if not provided

- step def - opts==opt

- opt is the standard (not opts)

- custom steps load as modules relative to spec file folder

